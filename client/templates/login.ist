#login
	.label-container
		label "login"
	input[type=text][!keyup=loginKeypress][!blur=blur]
	label.error

#password
	.label-container
		label "password"
	input[type=password][!keyup=passKeypress][!blur=blur]