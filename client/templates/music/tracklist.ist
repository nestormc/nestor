table.tracklist.datagrid
	tr
		th "#"
		th "title"
		th "duration"
		th "artist"
		th "album"
		
	tbody.tracks !tracks
		@each tracks
			tr
				td "{{ number }}"
				td "{{ title }}"
				td "{{ humanTime(duration) }}"
				td "{{ artists.join(', ') }}"
				td "{{ album }}"